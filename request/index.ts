import { suggestionList } from './engineSuggestion';
import { bookmarkSearch } from './bookmarkSearch';
import { historySearch } from './historySearch';

export const requestMap = {
  suggestionList,
  bookmarkSearch,
  historySearch,
};
export default requestMap;
